import { BacColorDirective } from './bac-color.directive';

describe('BacColorDirective', () => {
  it('should create an instance', () => {
    const directive = new BacColorDirective();
    expect(directive).toBeTruthy();
  });
});
