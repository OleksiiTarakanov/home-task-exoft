import { LettersDigitsDirective } from './letters-digits.directive';

describe('LettersDigitsDirective', () => {
  it('should create an instance', () => {
    const directive = new LettersDigitsDirective();
    expect(directive).toBeTruthy();
  });
});
